<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  

<mapper namespace="nbbang.mybatis.mapper.ussrdynamicsql">
	<select id="findUssrbbsWithTitleLike" parameterType="Map" resultType="ussrDTO">
		SELECT * FROM ussrbbs
		WHERE 1=1 
		<if test="ussrtitle != null">
			AND ussrtitle LIKE '%' || #{ussrtitle} || '%'
		</if>		
	</select>
	<select id="findUssrLike" parameterType="Map" resultType="ussrDTO">
		SELECT u.*,name FROM ussrbbs u JOIN member m ON u.email=m.email
		WHERE 1=1 
		<if test="ussrtitle != null">
			AND ussrtitle LIKE '%' || #{ussrtitle} || '%'
		</if>
		<if test="name != null">
			AND name LIKE '%' || #{name} || '%'
		</if>	
		<if test="ussrcontent != null">
			AND ussrcontent LIKE '%' || #{ussrcontent} || '%'
		</if>
	</select>
	<select id="findUssrLikeChoose" parameterType="Map" resultType="ussrDTO">
		SELECT u.*,name FROM ussrbbs u JOIN member m ON u.email=m.email
		WHERE 1=1
		<choose>
			<when test="ussrtitle != null">
				AND ussrtitle LIKE '%' || #{ussrtitle} || '%'
			</when>
			<when test="name != null">
				AND name LIKE '%' || #{name} || '%'
			</when>
			<otherwise>
				AND ussrcontent LIKE '%' || #{ussrcontent} || '%'
			</otherwise>		
		</choose> 	
	</select>
	<!-- 위 구문에서 "where 1=1"을 "where"로 변경시 조건에 따라 에러 발생 -->
	<!-- 아래 where 엘리먼트는 JSTL에 의해 컨텐츠가 리턴되면 단순히 "WHERE"만을 추가한다. 
	게다가 컨텐츠가 "AND"나 "OR"로 시작한다면 그 "AND"나 "OR"를 지워버린다. -->
	<select id="findUssrLikeWhere" parameterType="Map" resultType="ussrDTO">
		SELECT u.*,name FROM ussrbbs u JOIN member m ON u.email=m.email		
		<where>
			<if test="ussrtitle != null">
				AND ussrtitle LIKE '%' || #{ussrtitle} || '%'
			</if>
			<if test="name != null">
				AND name LIKE '%' || #{name} || '%'
			</if>	
			<if test="ussrcontent != null">
				AND ussrcontent LIKE '%' || #{ussrcontent} || '%'
			</if>		
		</where>
	</select>
	
	<select id="findUssrLikeTrim" parameterType="Map" resultType="ussrDTO">
		SELECT u.*,name FROM ussrbbs u JOIN member m ON u.email=m.email		
		<trim prefix="where" prefixOverrides="AND" >
			<if test="ussrtitle != null">
				AND ussrtitle LIKE '%' || #{ussrtitle} || '%'
			</if>
			<if test="name != null">
				AND name LIKE '%' || #{name} || '%'
			</if>	
			<if test="ussrcontent != null">
				AND ussrcontent LIKE '%' || #{ussrcontent} || '%'
			</if>		
		</trim>
	</select>
	<update id="updateUssrLikeTrim" parameterType="Map">
		UPDATE ussrbbs
		<trim prefix="SET" suffixOverrides=",">
			<if test="ussrtitle !=null">ussrtitle=#{ussrtitle},</if>
			<if test="ussrcontent !=null">ussrcontent=#{ussrcontent},</if>
			<if test="ussroriginfilenames !=null">ussroriginfilenames=#{ussroriginfilenames},</if>
			<if test="ussrrealfilenames !=null">ussrrealfilenames=#{ussrrealfilenames},</if>
		</trim>
		WHERE ussrno =#{ussrno}	
	</update>
	
	<update id="updateUssrSet" parameterType="Map">
		UPDATE ussrbbs
		<set>
			<if test="ussrtitle !=null">ussrtitle=#{ussrtitle},</if>
			<if test="ussrcontent !=null">ussrcontent=#{ussrcontent},</if>
			<if test="ussroriginfilenames !=null">ussroriginfilenames=#{ussroriginfilenames},</if>
			<if test="ussrrealfilenames !=null">ussrrealfilenames=#{ussrrealfilenames},</if>
		</set>
		WHERE ussrno =#{ussrno}		
	</update>
	
	<!-- List인 경우 -->
	<select id="findUssrForeach" parameterType="List" resultType="ussrDTO">
		SELECT * FROM ussrbbs
		WHERE ussrno IN 
		<foreach collection="collection" open="(" close=")" item="item" separator=",">
			#{item}
		</foreach>		
	</select>
	
	<delete id="deleteEmail" parameterType="int[]">
		DELETE ussrbbs
		WHERE ussrno IN
		<foreach collection="array" open="(" close=")" item="item" separator=",">
			#{item}
		</foreach>
	</delete>
	
</mapper>