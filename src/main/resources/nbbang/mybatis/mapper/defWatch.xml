<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="nbbang.mybatis.mapper.defWatch">

	<insert id="defWatchInsert" parameterType="defWatchDTO">
		INSERT INTO subfolder
		VALUES(SEQ_SUBFOLDER_SFNO.NEXTVAL,#{Watch},#{email})
	</insert>

</mapper> 