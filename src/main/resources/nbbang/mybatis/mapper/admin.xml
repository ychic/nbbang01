<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nbbang.mybatis.mapper.admin">
	<select id="selectAllMember" parameterType="Map" resultType="member">
		SELECT * FROM 
		(SELECT T.*,ROWNUM R FROM (SELECT * FROM member ORDER BY registdate DESC) T) 
		WHERE R BETWEEN #{start} AND #{end}
	</select>
	<select id="getTotalRowCount" resultType="int">
		SELECT COUNT(*) FROM member
	</select>
	<select id="getChatTotalRowCount" resultType="int">
		SELECT COUNT(*) FROM chat
	</select>
	<select id="selectAllChat" parameterType="Map" resultType="adminChat">
		SELECT * FROM
		(SELECT T.*,ROWNUM R FROM 
		(SELECT c.chatno, b.partyno, b.partytitle, c.participant, c.bbswriter, 
		(SELECT COUNT(*) from partymember 
		WHERE pmmember = c.participant AND partyno = c.partyno) member 
		FROM partybbs b JOIN chat c ON b.partyno = c.partyno
        ORDER BY c.chatno DESC) T)
        WHERE R BETWEEN #{start} AND #{end}		 
	</select>
</mapper>