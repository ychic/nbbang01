<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin.mybatis.mapper.admin">
	<select id="selectAllMember" resultType="adminMember">
		SELECT * FROM member ORDER BY registdate DESC
	</select>
</mapper>